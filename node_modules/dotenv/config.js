import { config } from './lib/main';
import envOptions from './lib/env-options';
import cliOptions from './lib/cli-options';

// Sammenlæg objekterne og kalder config
config({
  ...envOptions,
  ...cliOptions(process.argv)
});
